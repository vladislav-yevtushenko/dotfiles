# macOS specific settings

# Treat both Option keys as Alt/Meta for terminal apps
macos_option_as_alt both

# Allow remote control for theme switching and scripts
allow_remote_control yes

# Color management tuned for macOS
macos_colorspace srgb

