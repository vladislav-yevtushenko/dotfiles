# Fonts
font_family JetBrainsMono Nerd Font
# Fallback is handled by fontconfig; ensure FiraCode NF is installed as backup
# bold/italic variants auto-detected
bold_font        auto
italic_font      auto
bold_italic_font auto

font_size 15.0

# Ligatures enabled (default). Keep explicit for clarity
disable_ligatures never

