# Behavior, scrollback, selection

scrollback_lines 10000

# macOS style: copy selection to system clipboard
copy_on_select clipboard
strip_trailing_spaces smart

# Remember OS window size across restarts
remember_window_size yes

